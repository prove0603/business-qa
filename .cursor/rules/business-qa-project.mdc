---
description: Business QA 项目规范与架构指南
globs:
  - "**/*.java"
  - "**/*.vue"
  - "**/*.ts"
alwaysApply: true
---

# Business QA 项目规则

## 项目概述
基于 RAG 的业务智能问答助手。所有 AI 交互统一使用 Spring AI ChatClient。

## 技术栈
- **后端**：Java 21, Spring Boot 3.5.x, Spring AI 1.1.4, MyBatis-Plus 3.5.7, PostgreSQL + pgvector
- **前端**：Vue 3, TypeScript, Element Plus, Vite 6
- **AI**：Spring AI ChatClient + OpenAI EmbeddingModel（均通过 OpenAI 兼容接口指向 DashScope，已移除 DashScope SDK 依赖）
- **MCP**：Spring AI MCP Client（Streamable HTTP），连接 sentinel-ai MCP Server 获取 SQL 分析工具
- **RAG**：RetrievalAugmentationAdvisor + VectorStoreDocumentRetriever（相似度阈值 0.5，自动上下文注入）

## 包结构
- `com.zhuangjie.qa` — 根包
- `com.zhuangjie.qa.config` — Spring 配置（含 S3Config, StorageConfig, AiChatConfig）
- `com.zhuangjie.qa.common` — Result, PageResult
- `com.zhuangjie.qa.controller` — REST 控制器（前缀 /api/）
- `com.zhuangjie.qa.chat` — 对话引擎
- `com.zhuangjie.qa.doc` — 文档管理
- `com.zhuangjie.qa.file` — 文件基础设施（S3 存储、Tika 解析、内容类型检测、文本清洗）
- `com.zhuangjie.qa.change` — Git 变更检测 + AI 建议
- `com.zhuangjie.qa.guardrail` — AI 护栏机制（输入检查 + 输出脱敏）
- `com.zhuangjie.qa.rag` — RAG 基础设施（Embedding, VectorStore）
- `com.zhuangjie.qa.db.entity` — MyBatis-Plus 实体
- `com.zhuangjie.qa.db.mapper` — MyBatis-Plus Mapper
- `com.zhuangjie.qa.db.service` — MyBatis-Plus Service 层
- `com.zhuangjie.qa.pojo.req` — 请求对象（Java record）
- `com.zhuangjie.qa.pojo.vo` — 视图对象

## 编码规范
- 使用 Java 21 特性：record, sealed interface, pattern matching, text blocks
- 构造器注入：`@RequiredArgsConstructor`
- 请求/响应对象使用 Java record
- 所有 REST 接口统一返回 `Result<T>`
- 分页查询返回 `PageResult<T>`
- 代码和注释中禁止出现公司内部信息（本项目开源）
- GroupId: `com.zhuangjie`，禁止使用 `com.al.risk`
- JSON 序列化统一使用 Jackson（Spring Boot 自带），不要用 Hutool JSONUtil 序列化 record（5.8.x 不支持）

## AI 调用规范
- 所有 LLM 调用通过 Spring AI `ChatClient`（流式用 stream，同步用 call）
- Embedding 使用 Spring AI 的 `OpenAiEmbeddingModel`（text-embedding-v3, 1024 维，通过 DashScope OpenAI 兼容接口）
- 已移除自定义 `DashScopeEmbeddingModel` 适配器和 `dashscope-sdk-java` 依赖
- 禁止引入 spring-ai-alibaba；DashScope 使用 OpenAI 兼容接口
- 向量库：PgVectorStore，HNSW 索引，余弦距离，1024 维，表名 `qa_vector`
- `spring.ai.openai.base-url` 不能包含 `/v1` 后缀（Spring AI 会自动追加）
- 对话记忆 ID 通过 `ChatMemory.CONVERSATION_ID` 常量传入（不要硬编码字符串）
- 对话记忆实现：`MessageWindowChatMemory`（InMemoryChatMemory 在 Spring AI 1.0.0 中已移除）
- MCP 工具通过 `spring-ai-starter-mcp-client` 自动注册为 `ToolCallbackProvider`
- MCP Tools 不挂在 defaultTools 上，由 ChatService 按场景动态挂载（仅 SQL 相关问题时加载，减少 token 开销）
- MCP Client `initialized: false`（延迟初始化，Server 未启动不影响 Client 启动）
- MCP 传输协议：Streamable HTTP（无状态，Server 重启不影响 Client）
- RAG 使用 `RetrievalAugmentationAdvisor` 作为 Advisor 自动处理（不再手动拼接 context）
- 模块过滤通过 `VectorStoreDocumentRetriever.FILTER_EXPRESSION` 在每次请求时动态传入

## Prompt 模板管理
- 系统提示词存储在 `t_prompt_template` 表中，支持在线编辑
- `AiChatConfig` 启动时从 DB 加载 prompt，找不到则 fallback 到硬编码默认值
- `SuggestionGenerator` 每次调用时从 DB 加载 `ANALYSIS_USER` prompt 模板（支持热更新）
- 模板 key 约定：`CHAT_SYSTEM`（对话系统提示词）、`ANALYSIS_SYSTEM`（分析系统提示词）、`ANALYSIS_USER`（分析用户提示词）
- 用户提示词模板支持 `%s` 占位符，由调用方动态替换
- 前端管理页面：`/prompts`，支持 CRUD、启停、内容编辑
- **注意**：系统提示词（CHAT_SYSTEM、ANALYSIS_SYSTEM）修改后需重启服务生效（ChatClient 在启动时构建）

## 护栏机制（Guardrails）
- **输入护栏**：在 `ChatService.streamChat()` 中，调用 LLM 前先检查用户输入
  - `GuardrailService.checkInput()` 遍历活跃的 INPUT_KEYWORD / INPUT_REGEX 规则
  - 命中 BLOCK 规则：返回 `[GUARDRAIL]` 前缀的拦截消息（不调用 LLM）
  - 前端识别 `[GUARDRAIL]` 前缀，以警告样式展示拦截消息
- **输出护栏**：在 LLM 流式输出后，对每个 chunk 执行 `GuardrailService.filterOutput()`
  - 遍历 OUTPUT_REGEX 规则，对匹配的敏感信息（手机号、身份证等）自动脱敏
- **规则存储**：`t_guardrail_rule` 表，支持在线 CRUD，实时生效无需重启
- **规则类型**：INPUT_KEYWORD（关键词匹配）、INPUT_REGEX（正则匹配）、OUTPUT_REGEX（输出脱敏）
- **动作类型**：BLOCK（拦截）、WARN（警告）、MASK（脱敏）
- **测试接口**：`POST /api/guardrail/test` 可测试输入是否会触发规则
- 前端管理页面：`/guardrails`，含规则 CRUD + 实时测试功能

## 向量化流程
- `QaApplication` 上的 `@EnableAsync` 启用异步向量化
- 上传/创建/更新 → `VectorSyncService.asyncVectorize()`（异步线程池执行）
- `ChunkSplitter`：按 Markdown 标题分块，再按段落拆分，最大 1000 字符，最小 50 字符
- `VectorService.vectorizeDocument()`：删旧向量 → 分块 → Embedding → 存入 PgVectorStore
- 删除向量使用字符串过滤语法 `"document_id == " + id`（通过 `delete(String)`）
- `VectorService.search()` 使用 `FilterExpressionBuilder.in("module_id", ...)` 做模块范围过滤
- 分块数为 0 时，`vectorized` 保持 false

## 前端规范
- Composition API + `<script setup lang="ts">`
- API 层在 `src/api/index.ts`，axios 响应拦截器解包 `Result.data`
- SSE 流式通过 `fetch` + `ReadableStream`（不用 EventSource）
- 时间格式化统一使用 `utils/format.ts`
- Vite 代理：`/api -> http://localhost:8091`
- Element Plus 语言：`zh-CN`，所有界面文本使用中文

## 文件存储
- MinIO（S3 兼容）存储文档文件
- AWS SDK v2 S3Client，路径风格访问（`forcePathStyle=true`）
- Apache Tika 解析文档（PDF, Word, TXT, Markdown）
- 上传流程：校验 → Tika 检测类型 → Tika 提取文本 → 上传 MinIO → 元数据+解析内容存库 → 异步向量化
- 配置前缀：`qa.storage.*`（endpoint, access-key, secret-key, bucket, region）
- Bucket: `business-qa`，启动时自动创建

## 数据库
- PostgreSQL + pgvector 扩展
- 数据库名：`business_qa`
- 表名前缀 `t_`
- 向量表：`qa_vector`（PgVectorStore 自动创建，HNSW 索引，1024 维）
- `t_prompt_template` — Prompt 模板管理（template_key 唯一）
- `t_guardrail_rule` — 护栏规则（输入拦截 + 输出脱敏）
- 初始化数据脚本：`db/init-prompt-guardrail.sql`

## 配置注意事项
- `application.yml` 的 base-url：`https://dashscope.aliyuncs.com/compatible-mode`（不加 `/v1`）
- `application-dev.yml` 包含实际 API Key 和 GitLab PAT，已被 gitignore 排除
- `.vscode/launch.json` 通过 `AI_DASHSCOPE_API_KEY` 环境变量注入密钥
- MinIO 启动脚本在 `scripts/` 目录（ps1 + bat）

## 功能完成状态与规划

### 已完成
- 模块管理 CRUD（COMMON/TASK 类型）
- 文档管理（在线编辑 + 文件上传 + MinIO 存储 + Tika 解析）
- 文档分块 + 异步向量化
- RAG 智能问答（模块过滤 + SSE 流式 + 多轮对话记忆 + 引用来源）
- 仪表盘（四项统计指标）
- Git 变更检测 + AI 建议生成
- 变更建议查看/忽略
- **Prompt 模板管理**：系统/用户提示词在线编辑、启停、CRUD，从 DB 动态加载
- **AI 护栏机制（Guardrails）**：输入护栏（Prompt 注入防护、敏感词拦截）+ 输出护栏（手机号/身份证自动脱敏），规则在线管理 + 实时测试

### 待实现
- **应用建议自动修改文档**：点击"应用"后将建议内容写入文档正文，触发重新向量化
- **文档版本历史**：记录修改快照，支持回滚
- **Markdown 富文本编辑器**
- **仪表盘增强**：趋势图、模块统计、问答热度
- **向量化失败重试机制**
- **用户认证与多用户隔离**

## API 路径

- `/api/chat/*` — 对话（SSE 流式、会话 CRUD）
- `/api/module/*` — 模块管理
- `/api/document/*` — 文档管理（CRUD、上传、向量化）
- `/api/dashboard/*` — 仪表盘
- `/api/change/*` — 变更检测与建议
- `/api/prompt/*` — Prompt 模板管理（CRUD、启停、按 key 查询）
- `/api/guardrail/*` — 护栏规则管理（CRUD、启停、测试）
  - `POST /api/guardrail/test` — 测试输入是否触发护栏规则

## MCP 集成架构
- **business-qa（Client）** ←Streamable HTTP→ **sentinel-ai（Server）**
- sentinel-ai 通过 `@Tool` 注解暴露 SQL 分析工具（5 个），自动注册为 MCP Tools
- business-qa 的 `AiChatConfig` 通过 `ObjectProvider<ToolCallbackProvider>` 可选注入 MCP 工具
- LLM 根据用户问题自动判断是否调用工具（Function Calling），Spring AI 自动编排工具调用 → 结果返回 → 生成最终答案
- MCP 配置：`spring.ai.mcp.client.streamable-http.connections.sentinel-ai`
- sentinel-ai MCP Server 端口 8090，endpoint `/mcp`
